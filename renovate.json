{
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "release.yml is generated by dist and is checked against dist-workspace.toml by the 'plan' job. Editing it directly makes that check fail; action and runner versions there change by upgrading cargo-dist-version and regenerating.",
      "matchFileNames": [".github/workflows/release.yml"],
      "enabled": false
    }
  ]
}
